streamlit>=1.32.0
pydantic>=2.6.0
beautifulsoup4>=4.12.0
lxml>=5.1.0
pyyaml>=6.0.0
pytest>=8.0.0
httpx>=0.27.0
litellm==1.55.0
python-dotenv>=1.0.0

# Playwright + edgartools: install via requirements-docker.txt (Docker/Zeabur)
# or: pip install -r requirements-docker.txt when MSVC / compatible Python available
